# 引き継ぎ書 — 「土と薬」ホームページ制作

このフォルダは、陶芸作家「土と薬」のホームページ一式です。
Claude Cowork でこのフォルダを開き、この引き継ぎ書を読んでから続きの作業を進めてください。

---

## 1. プロジェクト概要

会津の薬剤師・ハシモトサトシが、趣味の陶芸（屋号「土と薬」）で
**会津本郷せと市（2026年6月14日）に出品**するにあたり作った、作家紹介＆告知用の1ページサイト。
スマホ・PC両対応の静的サイト（HTML/CSS/JSのみ。ビルド不要、そのまま公開できる）。

## 2. ブランド設定（文章・デザインの軸）

- **屋号**：土と薬（つちとくすり / tsuchi to kusuri）
- **作家**：ハシモトサトシ（薬剤師／プライマリ・ケア認定薬剤師、会津・みずき薬局）
- **コンセプト**：「薬剤師がつくる、ふだんのうつわ」。薬を扱う目で材料を確かめる癖が陶芸にも出ている。
- **見せ場のエピソード**：薪ストーブの灰から釉薬をつくる「薬剤師×陶芸」の実験。
- **作るもの**：豆皿・箸置き・飯碗・湯のみ・ぐい呑み・小鉢など、ふだん使いの小物中心。
- **トーン**：静かで温かい、和の craft。誇張せず、手づくりの正直さを大切に。砕けすぎず丁寧。

## 3. ファイル構成

```
hashimoto-satoshi-utsuwa/
├── index.html          … サイト本体（HTML+CSS+JSが1ファイルに収まっている）
├── HANDOFF.md          … この引き継ぎ書
└── images/
    ├── hero.jpg        … トップ背景用の横長写真（田園＋大皿を持つ本人）
    └── portrait.jpg    … プロフィール欄の縦長ポートレート（同じ写真の人物中心クロップ）
```

## 4. デザインシステム（変更時はこれに合わせる）

CSS変数（`index.html` の `:root` に定義）：
- 背景 `--paper:#f4efe4` / `--paper-deep:#ebe2d1` / カード `--paper-card:#f9f5ec`
- 文字 `--ink:#2a2521` / 補助 `--ink-soft:#6c645a` / 淡色 `--ash:#8c8675`
- アクセント（テラコッタ） `--clay:#a9542f` / 濃 `--clay-deep:#7c3c21`

フォント（Google Fonts）：
- 見出し：Shippori Mincho（明朝）
- 本文：Zen Kaku Gothic New
- 欧文アクセント：Cormorant Garamond（イタリック）

方針：和紙色の余白を広くとる／見出しは明朝／読み込み時と
スクロール時に `.reveal` 要素がふわっと出る（IntersectionObserver）。

## 5. セクション構成（index.html）

1. ヘッダー（屋号「土と薬」＋ナビ）
2. ヒーロー（キャッチ＋せと市への導線、背景に hero.jpg を薄く敷く）
3. つくり手（#about）— 人物紹介。portrait.jpg を使用
4. 薬剤師×陶芸（灰釉エピソードの見せ場、濃色背景）
5. うつわ（#works）— 6点のギャラリー。**現在はSVGイラストの仮置き**
6. せと市（#event）— 開催情報（2026/6/14 4:00–12:00 瀬戸町通り）
7. れんらく（#contact）— SNS・連絡先リンク
8. フッター

## 6. 完了済み

- 全体のデザイン・レイアウト・レスポンシブ対応
- 屋号「土と薬」を全箇所に反映
- 顔写真を ①プロフィール ②トップ背景 に組み込み済み
- リンク：X（@hayakuhantou）設定済み／note（ハッシュタグページ）設定済み

## 7. 残作業（TODO）— ここから続ける

1. **うつわギャラリーの写真差し替え（最優先）**
   - 現在 #works の6カードは各 `<div class="thumb"><svg>…</svg></div>` の仮イラスト。
   - 実物写真を `images/` に入れ、各 `<svg>…</svg>` を
     `<img src="images/mamezara.jpg" alt="豆皿">` のように差し替える。
   - 対象6点：豆皿 / 箸置き / 飯碗 / 湯のみ / ぐい呑み / 小鉢
   - 写真は正方形(1:1)で書き出すと枠にきれいに収まる。1枚あたり長辺1000px・JPEG品質85前後で軽量化推奨。
2. **Instagram のリンク**：#contact 内の `<a href="#" data-placeholder="instagram">` を
   実URLに変え、ラベルの「（準備中）」を外す。
3. **メールアドレス**：#contact の `mailto:your@email.example` を実アドレスに。
4. **Notionリンク（任意）**：作品ノートとして公開するなら、Notion側を「Web公開」にしてから
   #contact にリンクを追加（共有設定が非公開だと開けない）。
5. **公開**：下記メモ参照。

## 8. 公開の手順メモ

- **一番簡単**：Netlify Drop（https://app.netlify.com/drop）にこのフォルダごとドラッグ。即URL発行。
- **GitHub Pages**：リポジトリに push し、Pages を有効化。独自ドメインも設定可。
- どちらも `index.html` がトップとして自動表示される。ビルド作業は不要。

## 9. Cowork への頼み方（コピペ用の指示例）

> このフォルダは「土と薬」のホームページです。HANDOFF.md を読んで方針を把握してください。
> これから器の写真を images/ に追加するので、#works の6つのイラストを実写真に差し替えてください。
> デザインの色とフォントは HANDOFF.md の指定に合わせてください。

---
最終更新：2026年6月時点。作成者：ハシモトサトシ（土と薬）
